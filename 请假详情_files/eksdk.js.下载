define(function (require, exports, module) {
    var obj = {
        /**
         * 初始化
         */
        init: function () {
            var dfd = $.Deferred();
            dfd.reject("二开未设置SDK");
            return dfd.promise();
        },
    };
    return obj;
});